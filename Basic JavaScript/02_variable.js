const accountId = 1234567;
let name = "Shiv Kumar";
var city = "Patna";
state = "Bihar";
let country;

// By using const keyword we can't change that value

// It is preferred to use let instead of var

console.log(accountId, name, city, state);

console.table([accountId, name, city, state, country]);